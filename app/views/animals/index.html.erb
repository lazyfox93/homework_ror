<h1>Справочник животных</h1>
<p></p>
<p> количество типов животных в базе <%= @animals_count  %></p>
<p></p>
<%= link_to('Добавить новый тип или породу животного', new_animal_path)  %>
<p></p>
<table>
	<%- @animals.each do |animal| %>
	
	<tr>
		<td><%= animal.id %></td>
		<td><%= animal.animals_type %></td>
		<td><%= link_to(animal.breed, animal_path(id: animal.id)) %></td>
		<td><%= link_to('Редактировать', edit_animal_path(id: animal.id) ) %></td>
		<td><%= link_to('Удилить', animal_path(id: animal.id), method: :delete ) %></td>
	</tr>

	<%- end %>
</table>